import os.path


BROKER_HOST = "localhost"
BROKER_PORT = 5672
BROKER_VHOST = "/"
BROKER_USER = "guest"
BROKER_PASSWORD = "guest"

SQLALCHEMY_DATABASE_URI = 'postgresql+psycopg2:///tracker{{ year }}'

DATA_STORAGE_DIR = os.path.join(os.path.realpath(os.path.dirname(__file__)), '..', 'dq', 'data')

SECRET_KEY = "{{ iatidataquality_secret_key }}"

INDICATOR_GROUP = u"2014index"

CODELIST_API = "https://raw.githubusercontent.com/pwyf/iati-codelists/master/%s"

#SETUP_ORGS = ["undp", "dfid"]
#SETUP_PKG_COUNTER = 10

ORG_FREQUENCY_API_URL = "http://dashboard.iatistandard.org/timeliness_frequency.csv"
IATIUPDATES_URL = "http://{{ site_url }}/iatiupdates/api/package/hash/"

REMOVE_RESULTS = False
