import os.path


BROKER_HOST = "localhost"
BROKER_PORT = 5672
BROKER_VHOST = "/"
BROKER_USER = "guest"
BROKER_PASSWORD = "guest"

SQLALCHEMY_DATABASE_URI = 'postgresql+psycopg2:///tracker{{ year }}'

DATA_STORAGE_DIR = os.path.join(os.path.realpath(os.path.dirname(__file__)), '..', 'dq', 'data')

SECRET_KEY = "{{ iatidataquality_secret_key }}"

INDICATOR_GROUP = u"2014index"

CODELIST_API = "http://iatistandard.org/202/codelists/downloads/clv3/"

#SETUP_ORGS = ["undp", "dfid"]
#SETUP_PKG_COUNTER = 10

ORG_FREQUENCY_API_URL = "http://dashboard.iatistandard.org/timeliness_frequency.csv"
IATIUPDATES_URL = "http://{{ site_url }}/iatiupdates/api/package/hash/"

REMOVE_RESULTS = False

INTRO_HTML = 'Data collection for the {{ year }} Aid Transparency Index has now started. We will release more detailed information in {{ launch_date }} when the Aid Transparency Index will be launched. Results and analysis for previous years is available on the <a href="http://ati.publishwhatyoufund.org/" target="_blank">Aid Transparency Index site</a>.'

ATI_YEAR = "{{ year }}"